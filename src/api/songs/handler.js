/* eslint-disable class-methods-use-this */
class SongsHandler {
  constructor(service, validator) {
    this._services = service;
    this._validator = validator;

    
  }

  postSongHandler(req, h) {
    try {

    } catch (e) {

    }
  }
}

module.exports = SongsHandler;
